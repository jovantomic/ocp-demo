# Research views

`intelligence.js` connects watchlist, geography, history, signals and evidence. `lab.js` renders data ablation and hypothetical scenario comparisons. `portfolio.js` renders historical long/cash simulation results. `data-room.js` provides cutoff-filtered source inspection and export selection. Shared state and event handlers live in `../app.js`.
