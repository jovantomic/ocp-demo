# Shared components

`map.js` projects the supplied Natural Earth geography and renders date-specific synthetic layers. `charts.js` provides SVG history, keyboard/pointer inspection and portfolio equity comparison. `ui.js` contains formatting, tables and download helpers. All computation comes from `src/logic/engine.js`; chart components do not invent histories.
