{
  "name": "ocp-signal-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-data.mjs",
    "start": "npm run build && python3 -m http.server 4173 --bind 127.0.0.1 --directory dist",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "gather": "node --env-file-if-exists=.env scripts/gather.mjs"
  },
  "dependencies": {
    "csv-parse": "^7.0.2",
    "fast-xml-parser": "^5.11.1",
    "fflate": "^0.8.3",
    "ws": "^8.21.3"
  },
  "engines": {
    "node": ">=22"
  }
}
